# cargo-deny configuration.
# Run locally with `cargo deny check`. Run in CI via the deny job in ci.yml.

[graph]
all-features = true

[advisories]
version       = 2
yanked        = "warn"
ignore        = []

[bans]
multiple-versions = "warn"
wildcards         = "deny"

[licenses]
version = 2
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "CC0-1.0",
]
confidence-threshold = 0.93

[sources]
unknown-registry = "deny"
unknown-git      = "deny"
allow-registry   = ["https://github.com/rust-lang/crates.io-index"]
