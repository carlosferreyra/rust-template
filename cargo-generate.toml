[template]
cargo_generate_version = ">=0.21.0"
# system::command in the post hook requires --allow-commands at the CLI.
# `ignore` prevents files from being copied to the generated project.
# Hook files (hooks/*.rhai) must NOT be in ignore — cargo-generate reads them
# directly before generation. They are excluded from liquid processing only via `exclude`.
ignore = [
    "cargo-generate.toml.bak",
    "TEMPLATE.md",
    "PLAN.md",
    "CONTEXT.md",
    "CONTRIBUTING.md",
    "CHANGELOG.md",
    "release.toml",
    "dist-workspace.toml",
    "cliff.toml",
    ".cargo-dist",
    ".config",
    ".editorconfig",
    ".github",
    ".prettierignore",
    "AGENTS.md",
    "CLAUDE.md",
    "clippy.toml",
    "crates/README.md",
    "deny.toml",
    "examples",
    "rustfmt.toml",
    "scripts",
    "typos.toml",
    "LICENSE",
    ".claude",
]

exclude = [
    "hooks/pre-generate.rhai",
    "hooks/post-generate.rhai",
    "crates/xtask/src/scaffold.rs",
]

[hooks]
post = ["hooks/post-generate.rhai"]

[placeholders.github_username]
type    = "string"
prompt  = "GitHub username"
regex   = "^[a-zA-Z0-9]([a-zA-Z0-9-]{0,37})?$"

[placeholders.author_name]
type   = "string"
prompt = "Author name"

[placeholders.author_email]
type   = "string"
prompt = "Author email"

[placeholders.project_description]
type    = "string"
prompt  = "One-line description (optional)"
default = ""

[placeholders.license]
type    = "string"
prompt  = "SPDX license identifier (MIT, Apache-2.0, GPL-3.0-only, …)"
default = "MIT"
regex   = "^(AGPL-3\\.0-only|Apache-2\\.0|BSD-2-Clause|BSD-3-Clause|BSL-1\\.0|CC0-1\\.0|EPL-2\\.0|GPL-2\\.0-only|GPL-3\\.0-only|LGPL-2\\.1-only|MIT|MPL-2\\.0|Unlicense)$"
