[package]
name    = "xtask"
version = "0.0.0"
edition = "2024"
license = "MIT"
publish = false

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
clap      = { version = "4", features = ["derive"] }
toml_edit = "0.22"
